<UserControl x:Class="LearningCard.View.MainUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             MinHeight="300" MinWidth="300">
    <StackPanel Orientation="Vertical" VerticalAlignment="Center">
        <Button VerticalAlignment="Center" HorizontalAlignment="Center" Margin="10" Padding="5" Content="{Binding ActiveLanguage[StartNewQuiz]}" Command="{Binding ClickCommand_StartNewQnA}"/>
        <Button VerticalAlignment="Center" HorizontalAlignment="Center" Margin="10" Padding="5" Content="{Binding ActiveLanguage[StartOnlineGame]}" Command="{Binding ClickCommand_StartMultiplayer}"/>
        <Button VerticalAlignment="Center" HorizontalAlignment="Center" Margin="10" Padding="5" Content="{Binding ActiveLanguage[JoinOnlineQuiz]}" Command="{Binding ClickCommand_JoinMultiplayer}"/>
        <Button VerticalAlignment="Center" HorizontalAlignment="Center" Margin="10" Padding="5" Content="{Binding ActiveLanguage[CreateNewQuiz]}" Command="{Binding ClickCommand_CreateNewQnA}"/>
    </StackPanel>
</UserControl>
