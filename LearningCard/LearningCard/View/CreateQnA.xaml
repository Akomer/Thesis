<UserControl x:Class="LearningCard.View.CreateQuest"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             MinHeight="300" MinWidth="300">
    <StackPanel Orientation="Horizontal" VerticalAlignment="Stretch">
        <Grid VerticalAlignment="Stretch" MinHeight="300" MinWidth="250">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" MinHeight="125"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto" MinHeight="125"/>
                <RowDefinition Height="auto" MinHeight="50"/>
            </Grid.RowDefinitions>
            <UserControl Grid.Row="0" />
            <GridSplitter Grid.Row="1" Grid.ColumnSpan="2" />
            <UserControl Grid.Row="2" />
            <StackPanel Grid.Row="3" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                <Button VerticalAlignment="Center" HorizontalAlignment="Stretch" Content="Add Card" Margin="10" Padding="5"/>            
            </StackPanel>
        </Grid>
        <ListBox Width="100" VerticalAlignment="Stretch" Margin="10">

        </ListBox>
    </StackPanel>
</UserControl>
